export const DEFAULT_BOARD_WIDTH = 25;
export const DEFAULT_BOARD_HEIGHT = 25;
export const DEFAULT_BOARD_SIZE = 32;

export const environment = {
  DEFAULT_BOARD_WIDTH,
  DEFAULT_BOARD_HEIGHT,
  DEFAULT_BOARD_SIZE,
};
